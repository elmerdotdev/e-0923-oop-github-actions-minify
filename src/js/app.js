// Add function
function add(a, b) {
  return a + b;
}

// Subtract function
function sub(a, b) {
  return a - b;
}

// Multipy function
function mul(a, b) {
  return a * b;
}

// Divide function
function div(a, b) {
  return a / b;
}

document.querySelector('.container').style.backgroundColor = 'red'

